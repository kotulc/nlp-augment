# Changelog

## 0.1.0 - Unreleased
- Rebuilt the CLI around normalized JSON input/output contracts.
- Added strict validation and consistent JSON error payloads.
- Added provider registry/factory with config/env/CLI selection precedence.
- Added deterministic mock providers for stable local/CI behavior.
- Added command contract regression tests across list/dict/grouped input variants.
- Added executable example files and documentation integration tests.
- Removed unused legacy provider implementation modules under `src/mdaug/providers/default`.
